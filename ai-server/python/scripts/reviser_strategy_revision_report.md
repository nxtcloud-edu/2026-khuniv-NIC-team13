# EXAONE 자기소개서 Reviser 수정률·Latency 비교 보고서

- 입력: `scripts/upstage_compare_score_reviser_4096_result.json`에 저장된 동일 3문항
- 모델: `LGAI-EXAONE/K-EXAONE-236B-A23B` (Friendli OpenAI-compatible API)
- 운영 확정 설정: 첫 생성 `thinking=false, temperature=0`, 교정 생성 `thinking=false, temperature=1.0, top_p=0.95`, 문항별 최대 5회, 호출당 최대 4,096토큰
- 실제 수정 판정: 원문과 불일치하고 100자 이상 답변의 SequenceMatcher 유사도가 0.92 이하이며 모든 코드 검증을 통과한 경우

## 결론

- 변경 전 저장 결과는 3문항 모두 원문과 완전히 같았습니다. 비교용 7회 한도 실측에서는 19/21개(90.5%)가 실제 수정됐습니다.
- 5회 한도 대비 수정률은 86.7% → 90.5%, 평균 latency는 24.886초 → 25.437초(+2.2%)였습니다.
- 7회 한도 p95는 38.755초로 5회 한도보다 +46.7% 증가했고, 동일한 `6개월간` 오류가 반복된 실행은 최대 42.859초 후에도 원문 fallback됐습니다.
- 결론적으로 7회는 수정률을 소폭 높였지만 반복 불가능한 오류를 해결하지 못하며 꼬리 지연을 키웁니다.

## 실측 비교

| 설정 | 실행 | 실제 수정 | 원문 fallback | 교정 호출 | 평균 후보 호출/실행 | 평균 | p95 | 최대 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 변경 전 저장 결과 | 1 | 0/3 (0.0%) | 2 | 2 | - | 13.869s | 13.869s | 13.869s |
| 샘플링-only · 최대 5회 | 5 | 11/15 (73.3%) | 4 | 44 | 11.80 | 28.221s | 32.601s | 33.302s |
| 하이브리드 · 최대 5회 | 5 | 13/15 (86.7%) | 2 | 32 | 9.40 | 24.886s | 26.422s | 26.425s |
| 하이브리드 · 최대 7회 | 7 | 19/21 (90.5%) | 2 | 45 | 9.43 | 25.437s | 38.755s | 42.859s |

## 현재 LLM에 전달되는 수정 정보

- Evaluate의 전략명을 원문 안전 편집 동작으로 변환한 `editing_plan`
- 결론 우선 배치, 인과관계 연결, 문장 순서 재구성, 수치·성과 배치, 직무 연결 중 적용 가능한 작업
- 문항별 질문·원문, 목표 글자 수, 반드시 보존할 숫자와 영문 기술명
- 원문에 없어 사용하면 안 되는 행동·기술 표현
- 재시도 시 직전 후보와 알고리즘이 검출한 `problems_to_fix`

재시도 여부는 LLM이 아니라 Python 검증기가 결정합니다. LLM은 수정 후보만 생성하고, 코드가 동일/근접 복사·숫자 누락·새 사실·깨진 문자·문장 파편·중복·분량을 검사합니다.

## 정성 품질 감사

코드 검증 기준으로는 19/21개가 수정됐지만, 사람이 결과를 다시 읽었을 때 최소 5개 답변에서 의미 귀속 위험을 확인했습니다.

| 실행/문항 | 관찰 | 위험 |
|---|---|---|
| 2/2 | `6개월간 인턴십`을 `6개월간 지속된 문제 해결 과정`으로 연결 | 기간의 대상 변경 |
| 5/2, 6/2, 7/2 | 원문에 없는 `삼성전자 백엔드 인턴십`으로 표현 | 회사 정보가 지원자 경력에 잘못 귀속 |
| 7/1 | 원문의 `DS부문이 다루는`을 `DS부문이 주도하는`으로 강화 | 회사 사실 강도 상승 |

따라서 90.5%는 구조적 수정률이며 최종 제출 품질 통과율로 해석하면 안 됩니다. 위에서 식별한 5건을 제외한 보수적 수동 검토 통과 하한은 16/21(76.2%)입니다.

## 대표 결과 — 운영 5회 설정 실행 3

### 문항 1 — 유사도 0.7537

원문: 저는 대규모 반도체 생산 설비에서 발생하는 데이터를 실시간으로 처리하는 백엔드 시스템 설계에 관심이 많아 삼성전자 DS부문 소프트웨어 개발 직무에 지원했습니다. 학부 캡스톤 프로젝트에서 초당 5,000건 이상의 센서 이벤트를 Kafka와 분산 캐시로 처리하는 파이프라인을 설계하며, 대용량 트래픽 환경에서의 안정성과 확장성 문제에 깊이 매료되었습니다. 삼성전자 DS부문이 다루는 설비 자동화(FA) 및 제조 실행 시스템(MES) 영역에서 이러한 경험을 바탕으로 기여하고 싶습니다.

수정: 삼성전자 DS부문 소프트웨어 개발 직무에 지원한 동기는 대규모 반도체 생산 설비에서 발생하는 데이터를 실시간 처리하는 백엔드 시스템 설계에 대한 깊은 관심에서 비롯됩니다. 학부 캡스톤 프로젝트를 통해 초당 5,000건 이상의 센서 이벤트를 Kafka와 분산 캐시로 처리하는 파이프라인을 설계하며, 대용량 트래픽 환경에서의 안정성과 확장성 문제에 깊이 매료되었습니다. 삼성전자 DS부문이 다루는 설비 자동화(FA) 및 제조 실행 시스템(MES) 영역에서 이러한 경험을 바탕으로 기여하고 싶습니다.

### 문항 2 — 유사도 0.8157

원문: 저의 강점은 문제를 근본 원인부터 파고드는 끈기와 데이터 기반 의사결정 능력입니다. 6개월간 진행한 백엔드 인턴십에서 특정 API의 평균 응답 시간이 800ms까지 증가하는 문제를 발견했습니다. APM 도구로 프로파일링을 진행한 결과 N+1 쿼리 문제와 불필요한 캐시 미스가 원인임을 확인했고, 쿼리 배치 처리와 캐시 정책 개선을 통해 응답 시간을 120ms로 약 85% 단축시켰습니다. 이 경험을 통해 정량적 근거를 바탕으로 문제를 해결하는 역량을 키웠습니다.

수정: 제 강점은 문제의 근본 원인을 끈기 있게 분석하고 데이터 기반 의사결정을 통해 해결하는 능력입니다. 6개월간 진행한 백엔드 인턴십에서 특정 API의 평균 응답 시간이 800ms까지 증가하는 문제가 발생했습니다. APM 도구로 프로파일링을 수행한 결과, N+1 쿼리 문제와 불필요한 캐시 미스가 주요 원인임을 확인했습니다. 이를 바탕으로 쿼리 배치 처리를 적용하고 캐시 정책을 개선한 결과, 응답 시간을 120ms로 약 85% 단축시켰습니다. 이 과정에서 정량적 근거를 바탕으로 문제의 구조적 원인을 해결하는 역량을 구체적으로 발휘했습니다.

### 문항 3 — 유사도 0.9087

원문: 입사 후 첫 2년은 제조 시스템의 데이터 파이프라인과 백엔드 아키텍처를 깊이 이해하는 데 집중하고, 이후 3년 동안은 실시간 설비 이상 감지 시스템 고도화 프로젝트를 주도하고 싶습니다. 이를 위해 사내 기술 세미나와 사외 컨퍼런스를 통해 분산 시스템 및 MLOps 관련 지식을 지속적으로 학습하고, 사내 스터디 그룹을 조직해 팀 전체의 기술 역량 향상에도 기여할 계획입니다.

수정: 입사 후 5년 이내의 목표는 실시간 설비 이상 감지 시스템의 고도화이며, 이를 위해 첫 2년은 제조 시스템의 데이터 파이프라인과 백엔드 아키텍처를 깊이 이해하는 데 집중하고, 이후 3년 동안은 실시간 설비 이상 감지 시스템 고도화 프로젝트를 주도하고 싶습니다. 이를 위해 사내 기술 세미나와 사외 컨퍼런스를 통해 분산 시스템 및 MLOps 관련 지식을 지속적으로 학습하고, 사내 스터디 그룹을 조직해 팀 전체의 기술 역량 향상에도 기여할 계획입니다.

## 권고

- 5회·7회 실측 비교 후 운영 기본값은 문항별 최대 5회로 확정해 코드에 반영했습니다.
- 운영 적용 범위를 넓히기 전, 숫자 수식 대상과 회사/지원자 사실 귀속을 문장 단위로 검증하는 보완이 우선입니다.
- 동일 오류 재시도는 2~3회 이후 효율이 급격히 낮으므로 오류 유형별 결정적 복원 또는 더 좁은 필드 교정이 latency 개선에 유리합니다.
- 표본이 5회와 7회로 작고 EXAONE 샘플링 변동이 있으므로 수정률 차이를 통계적으로 확정하지 않습니다.

## 원시 측정 파일

- `scripts/reviser_strategy_revision_5runs.json` — 샘플링-only 5회 한도 중간 실험
- `scripts/reviser_strategy_revision_hybrid_5runs.json` — 유지된 하이브리드 5회 한도 실험
- `scripts/reviser_strategy_revision_final_5runs.json` — 폐기한 숫자 문맥/최적 후보 실험
- `scripts/reviser_strategy_revision_7attempts_7runs.json` — 비교용 7회 한도/7회 반복 실험
